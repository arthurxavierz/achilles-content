import { audit,body,json,method,requireAdmin,safeError,text } from './_shared.js'
export async function handler(event){try{method(event);const auth=await requireAdmin(event);const input=await body(event);const id=text(input.user_id,80,true);if(typeof input.active!=='boolean')throw Object.assign(new Error('Status inválido'),{statusCode:400});const {error}=await auth.service.from('profiles').update({active:input.active}).eq('id',id);if(error)throw error;await audit(auth.service,auth.user.id,id,input.active?'client_activated':'client_deactivated',{});return json(200,{ok:true})}catch(error){return safeError(error)}}
