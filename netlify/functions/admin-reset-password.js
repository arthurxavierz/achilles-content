import { audit,body,json,method,requireAdmin,safeError,text } from './_shared.js'
const make=()=>{const chars='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$';const bytes=crypto.getRandomValues(new Uint32Array(16));return Array.from(bytes,n=>chars[n%chars.length]).join('')}
export async function handler(event){try{method(event);const auth=await requireAdmin(event);const input=await body(event);const id=text(input.user_id,80,true),password=make();const {error}=await auth.service.auth.admin.updateUserById(id,{password});if(error)throw error;await audit(auth.service,auth.user.id,id,'password_reset',{});return json(200,{password})}catch(error){return safeError(error)}}
