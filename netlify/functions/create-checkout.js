import { body,json,method,requireUser,safeError,text } from './_shared.js'
export async function handler(event){try{method(event);const auth=await requireUser(event);const input=await body(event);if(!process.env.MP_ACCESS_TOKEN)throw Object.assign(new Error('Checkout automático não está habilitado. Use o PIX.'),{statusCode:503});const kind=input.kind,slug=text(input.slug,80,true);if(!['plan','credit_pack'].includes(kind))throw Object.assign(new Error('Tipo de compra inválido'),{statusCode:400});const table=kind==='plan'?'plans':'credit_packs';const {data:item}=await auth.service.from(table).select('*').eq('slug',slug).eq('active',true).single();if(!item)throw Object.assign(new Error('Item não encontrado'),{statusCode:404});const amount=item.price_cents,credits=kind==='plan'?item.monthly_credits:item.credits;const payload={user_id:auth.user.id,kind,provider:'mercadopago',amount_cents:amount,credits,status:'pending',[kind==='plan'?'plan_id':'pack_id']:item.id};const {data:payment,error}=await auth.service.from('payments').insert(payload).select('*').single();if(error)throw error;const pref=await fetch('https://api.mercadopago.com/checkout/preferences',{method:'POST',headers:{authorization:`Bearer ${process.env.MP_ACCESS_TOKEN}`,'content-type':'application/json','x-idempotency-key':payment.id},body:JSON.stringify({items:[{title:`Achilles Content | ${item.name}`,quantity:1,unit_price:amount/100,currency_id:'BRL'}],external_reference:payment.id,back_urls:{success:`${process.env.APP_URL}/app/planos?pagamento=sucesso`,pending:`${process.env.APP_URL}/app/planos?pagamento=pendente`,failure:`${process.env.APP_URL}/app/planos?pagamento=falha`},notification_url:`${process.env.APP_URL}/.netlify/functions/mp-webhook`})});if(!pref.ok)throw new Error(`Mercado Pago ${pref.status}`);const data=await pref.json();await auth.service.from('payments').update({mp_preference_id:data.id}).eq('id',payment.id);return json(200,{checkout_url:data.init_point||data.sandbox_init_point})}catch(error){return safeError(error)}}
