import { json,method,requireAdmin,safeError } from './_shared.js'
export async function handler(event){try{method(event,['GET']);const auth=await requireAdmin(event);const {data,error}=await auth.service.from('profiles').select('id,full_name,email,active,credits,credits_plan,credits_extra,created_at').eq('role','client').order('created_at',{ascending:false});if(error)throw error;return json(200,{clients:data||[]})}catch(error){return safeError(error)}}
